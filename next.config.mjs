/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'images.pexels.com', // Pexels image domain
      ],
    },
  };
  
  export default nextConfig;
  